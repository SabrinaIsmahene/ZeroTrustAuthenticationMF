import { createHolderDID } from "./holderDID"
import { verifyVC, saveVC } from "./holderVC"
import type { serverInfo, VerifiedVC } from "./types"

export { createHolderDID, verifyVC, saveVC }
export type { serverInfo, VerifiedVC }
